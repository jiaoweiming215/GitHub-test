1.添加节点时，无法申请内存解决方案：
a.优先检查map文件heap空间分配，如无空间，在keil环境中需要配置.S文件的heap size
  IAR环境中需要在icf文件中配置或者编译器中界面配置heap size。
b.如工程中包含了自己编写的malloc函数，可能有误，屏蔽此函数，优先选择使用microlib中的malloc和free函数

2.单链表
尾节点指向空

3.单循环链表：
尾节点指向头结点

4.双向循环链表：
a.申请prior指针指向节点前地址，添加删除节点时需逻辑清晰，对指针的赋值需要明确。

